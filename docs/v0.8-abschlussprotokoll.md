# in:si 0.8 – Abschlussprotokoll des Funktionsumfangs

Stand: 27. August 2026 · Version: `0.8.0.dev0` · Zweig: `develop/v0.8`

Dieses Protokoll hält den Umfangsschnitt vor der Freigabe von 0.8 fest. Es
ersetzt weder die technische Historie in der [Roadmap](../ROADMAP.md) noch die
Plattformmatrix in der [Qualitätssicherung](../QUALITAETSSICHERUNG.md), sondern
ordnet deren aktuellen Stand in umgesetzt, nachgewiesen und noch offen ein.

## Entscheidung

Der Funktionsumfang von 0.8 ist geschlossen. Bis zur Freigabe werden keine
weiteren Produktfunktionen aufgenommen. Zulässig sind nur:

- Korrekturen, die aus den verbleibenden Freigabeprüfungen entstehen;
- notwendige Dokumentations-, Versions- und Packagingkorrekturen;
- kleine Vereinfachungen mit unmittelbar belegbarem Nutzen und geringem
  Regressionsrisiko.

Große neue Fachmodule, globale projektübergreifende Ergebnisse, ein gemeinsamer
Assetbereich, Mehrbenutzerprofile und weitere Projektversionsfunktionen bleiben
in den späteren Meilensteinen. Nach 0.8 haben die Fertigstellung der vorhandenen
Grundkurse und deren Unterrichtspilot Vorrang vor einer erneuten Verbreiterung
der Plattform. Auch große Module werden vor 0.8 nicht allein wegen ihrer
Zeilenzahl umgebaut. Insbesondere folgt auf die bestehende Sandbox zuerst die
reale Plattformprüfung; umgebaut wird dort nur aufgrund eines konkreten
Sicherheits-, Build- oder Wartungsbefunds.

## Umgesetzter 0.8-Umfang

| Bereich | Ergebnis |
|---|---|
| Datenmigration | Versionierte, idempotente Migration von 0.7-Daten mit unverändertem Originalbackup und atomarem Versionsmarker |
| Kurs-Runtime | Neue Generationen werden vollständig aufgebaut und geprüft, bevor ein atomarer Marker sie aktiviert |
| Projektstände | Automatische und benannte Stände mit Kommentar, Prüfsummen, Aufbewahrung und sicherer Wiederherstellung |
| Lokale Daten | Portabler Gesamtexport sowie getrennte, exakt bestätigte Papierkorbaktion für erreichbare Kurs- und App-Daten |
| Kurs- und Inhaltsupdates | Gemeinsame atomare Aktivierung, konsolidierte Importtransaktionen und bewusst manuelle Netzwerkprüfungen |
| Fachmodule | Neutraler Trainervertrag, spät geladener PyKIM-Adapter und versionsbezogene Zustimmung für externe Trainer-Erweiterungen |
| Autorenoberfläche | Ein kanonischer Kurseditor, sichtbare Aufgabenarten und gestufte Hinweise für freie und geprüfte Aufgaben |
| Projekte | Getrennte Sprite- und Musikeditoraktionen mit Erkennung sofort fehlgeschlagener Starts |
| Performance | Keine künstliche Mindestwartezeit, verzögert aufgebaute Tabs, vorab geladene Runtimeprüfung und weniger redundante Datei-/Prozessprüfungen |
| Packaging | in:si-Namen für Specs und Buildartefakte, exakt gesetzte direkte Abhängigkeiten, Buildmanifest und verkleinertes Offline-Wheelhouse |
| Wartbarkeit | Architekturgrenzen und Größenbudgets, getrennte Archiv-/Speicherlogik sowie fachlich aufgeteilte Integrationsprüfungen |

## Nachweis am Umfangsschnitt

- Paketmetadaten und Laufzeit melden konsistent `0.8.0.dev0`; der
  Release-Versionscheck bestätigt denselben Stand.
- 476 reguläre Prüfungen bestehen. Eine echte Bubblewrap-Prüfung wird auf macOS
  erwartungsgemäß übersprungen, weil sie Linux und `bwrap` benötigt.
- Alle vier separat markierten NiceGUI-E2E-Abläufe bestehen lokal.
- `test_guide.py` sank im Verlauf der Konsolidierung von 2.862 auf 1.438 Zeilen;
  Runtime-/IDE-/Pyxel- sowie Inhalts-/Update-/Zertifikatsverträge sind getrennt
  ausführbar.
- Der synchrone Seitenaufbau eines Kurses sank in der dokumentierten lokalen
  Vergleichsmessung von rund 0,37 auf 0,14 Sekunden. Die Runtimeerkennung von
  acht Kandidaten sank von rund 0,42 auf 0,14 Sekunden.
- Der lokale macOS-ARM-DMG-Prototyp sank von rund 113 MB auf rund 82 MiB. Das
  Ziel unter 100 MB ist für die übrigen drei Plattformartefakte noch nicht
  nachgewiesen.
- Der selbst gepflegte Python-Produktivcode umfasst 18.793 Zeilen gegenüber
  18.746 Zeilen zu Beginn: netto +47 Zeilen beziehungsweise rund +0,3 Prozent.
  Das ursprüngliche Netto-Reduktionsziel wurde damit nicht erreicht. Der nahezu
  konstante Umfang enthält jedoch zusätzlich Projektwiederherstellung und
  vollständige lokale Datenkontrolle; eine künstliche Verdichtung erfolgt vor
  der Freigabe nicht.

## Bewusst verbleibende Wartungsschuld

Die größten selbst gepflegten Python-Module sind weiterhin `sandbox.py` mit
1.408, `runtime.py` mit 863, `course_studio_view.py` mit 849 und
`windows_sandbox_helper.py` mit 827 Zeilen. Weitere View-Module liegen zwischen
rund 650 und 750 Zeilen. Architekturtests begrenzen mehrere dieser Bereiche,
aber nicht jede Größe ist bereits eine gute fachliche Grenze.

Diese Schuld wird nicht verschwiegen, ist für 0.8 aber kein eigenständiger
Freigabeblocker. Vor der Freigabe haben Verhaltensstabilität und reale
Plattformnachweise Vorrang. Weitere Zerlegung gehört in einen eigenen, kleinen
Änderungsschnitt nach 0.8 oder entsteht aus einem konkreten Prüfbefund.

## Noch offen vor `v0.8.0`

1. Reale 0.7-Beispieldaten sowie Backup, Migration und Wiederherstellung auf
   tatsächlich entfernbaren Datenträgern prüfen.
2. Windows x86_64, Linux x86_64, macOS Intel und macOS ARM reproduzierbar bauen;
   Paketmanifest, Offline-Runtime-Aufbau und komprimierte Größe je Artefakt
   protokollieren.
3. Die nativen Sandbox-Selbsttests einschließlich Ressourcen- und
   Prozessgrenzen auf den jeweiligen Zielsystemen ausführen; grafische Starts
   unter Windows, macOS und Linux/Wayland prüfen. Das grüne `v0.7.1`-Release
   belegt AppContainer-Selbsttest und echten Windows-Fensterstart; beide müssen
   auf dem zusammengeführten 0.8-Stand und einem echten Windows-Schulgerät
   erneut bestätigt werden.
4. Toolbar, Kurswechsel, Setup, Aufgaben, Projektstände, Datenexport/-löschung,
   Sprite-/Musikeditor und Stoppen laufender Programme auf echten Zielgeräten
   manuell abnehmen.
5. Den Entwicklungsstand über Pull Request beziehungsweise Merge durch die
   CI-Prüfungen führen. Pushes auf `develop/v0.8` lösen die derzeitigen
   Workflows nicht aus; die lokalen E2E-Prüfungen ersetzen diesen Nachweis
   nicht.
6. Erst nach grüner Matrix `0.8.0.dev0` in Paket- und Laufzeitmetadaten auf
   `0.8.0` setzen, den Versionscheck mit `v0.8.0` ausführen und den Release-Tag
   aus dem geprüften Commit erzeugen.

## Abschlusskriterium

0.8 ist freigabefähig, wenn alle sechs Punkte protokolliert grün sind oder eine
Abweichung ausdrücklich als bekannte, sichere Produktgrenze dokumentiert ist.
Bis dahin bleibt 0.7.1 die stabile Downloadversion und `develop/v0.8` ein
Entwicklungszweig.
